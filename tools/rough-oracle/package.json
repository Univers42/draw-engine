{
  "name": "rough-oracle",
  "private": true,
  "type": "module",
  "description": "Dumps rough.js's own output as fixtures so the Rust port can be diffed against it. Deliberately OUTSIDE the pnpm workspace: it pins roughjs to the exact version Excalidraw uses, which is the whole point, and must not inherit the workspace's minimumReleaseAge hold.",
  "scripts": {
    "generate": "node --import ./register.mjs generate.mjs"
  },
  "dependencies": {
    "roughjs": "4.6.4"
  }
}
